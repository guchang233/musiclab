{
  "$schema": "https://schema.tauri.app/acl/capability",
  "identifier": "default",
  "description": "MusicLab 主窗口默认权限",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default"
  ]
}
