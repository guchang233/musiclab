[package]
name = "musiclab-ui"
version = "0.1.0"
description = "MusicLab 桌面壳：拉起核心服务进程并呈现 UI"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
default = []
