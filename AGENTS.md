# 项目规则（AI Agent 必须遵守）

本文件对仓库内所有 AI 会话生效。Agent 在开始任何工作时必须先读取本文件，
并在会话结束时执行「上下文留痕」流程（见 [§会话留痕](#会话留痕-强制)）。

## 项目概览（简短）

音乐工作站内核 `musiclab`：音源多轨分离 + WAV→MIDI 转录引擎。

- 分层架构：`CLI → pipeline（编排+缓存） → engine（分离/转录后端）`。
- ML 后端为可选依赖，缺失时自动降级到 DSP 后端。
- 重点：核心能力优先于 UI；可扩展、可维护、健壮。

## 代码规范

- 语言：注释与文档用中文；代码标识符用英文。
- 分层：UI/CLI 不许直接触碰 engine 内部，必须经 pipeline。
- 后端以 `BackendSpec` + 能力矩阵注册，新增能力走注册表，不改核心编排。
- 新功能优先以「插件/后端」形式接入，避免耦合进核心。
- 任何外部依赖尽量做成可选，核心包保持轻量。
- 改动必须保证现有测试通过：`pytest -q`。

## 会话留痕（强制）

**目的**：任何 AI 会话结束后，上下文必须沉淀到仓库文件，供后续会话恢复，不依赖聊天记录。

### 1. 每轮会话结束必须做的事

在会话结束时（回答用户「完成」前），必须在 `docs/sessions/` 下追加一条记录。具体：

- **追加写法**：新建带时间戳的条目文件，追加到 `docs/sessions/log.md`（见下方格式）。
- **文件命名**：`docs/sessions/<YYYYMMDD-HHMM>.md`（按每轮会话新建，避免串扰）。
- **结构模板**（每条记录含以下字段，缺省标 `无`）：

```markdown
## <会话时间> - <一句话目标>

- **目标**：<本轮任务>
- **上下文锚点**：<进入会话时的关键状态，如待办、分支、最近提交 sha>
- **关键改动**： <路径+做法，逐条列出>
- **决策与理由**：<架构/技术取舍>
- **踩坑 / 修复**：<错误现象 → 根因 → 修复>
- **验证结果**：<测试、用例数、端到端数据>
- **遗留 / 下一步**：<待办、建议后续动作>
```

### 2. 恢复上下文（会话开始时）

- 先读本文件与 `docs/sessions/log.md` 末尾若干条，若无则告知用户无历史。
- 依据最近记录的「遗留/下一步」或「上下文锚点」续接，不重复已解决项。

### 3. 留痕链路（本仓库约定）

| 环节 | 位置 |
| --- | --- |
| 项目规则 | `AGENTS.md` |
| 会话日志索引 | `docs/sessions/log.md` |
| 每条明细 | `docs/sessions/<时间戳>.md` |
| 模板 | `docs/sessions/TEMPLATE.md` |

### 4. 坚持以下原则

- **留痕早于收尾**：先写留痕，再回复用户「完成」。
- **不膨胀**：每条记录简洁聚焦，必要时引用代码路径即可
  ，不复制大段代码。
- **不重复**：已被历史条目覆盖且未再改动的事项，写明「无变化」，不再展开。
- **会话中途若被中断**：如已完成阶段性工作，同样补一条中间态留痕，
  保证上下文不因中断丢失。