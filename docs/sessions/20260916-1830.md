## 2026-09-16 18:3X - CI：自动测试 / 自动编译 / 自动发布 Release

- **目标**：为仓库添加 GitHub Actions——CI 测试与打 tag 自动编译并发布 Release。
- **上下文锚点**：commit `f106423`（服务层 + Tauri 壳骨架）；122 用例基线。
- **关键改动**：
  - `.github/workflows/ci.yml`（新）：push(main)/PR 触发——Python 3.10/3.12/3.13 矩阵跑 `pytest -q`；`python -m build` + `twine check` 构建检查。
  - `.github/workflows/release.yml`（新）：tag `v*` / workflow_dispatch 触发——test → build-python（sdist+wheel artifact）∥ build-tauri（ubuntu-22.04 装 webkit2gtk-4.1 等系统依赖，tauri-cli v2 构建 deb，continue-on-error 不阻塞发布）→ release（softprops/action-gh-release 自动生成 release notes，上传 wheel/tar.gz/deb）。
- **决策与理由**：
  - Tauri 构建独立 job 且 `continue-on-error: true`：系统依赖链复杂、沙箱无法预验证，失败只影响 deb 附件，不阻塞 Python 包发布。
  - 发布用 tag 驱动而非每次 push：release 语义明确，`GITHUB_TOKEN` 自带 `contents: write` 权限，无需用户 token。
  - CI 矩阵含 3.10（pyproject 下限）到 3.13，防语法/typing 兼容回归。
- **踩坑 / 修复**：
  - 沙箱无 pyyaml/build → 安装后本地验证两个 workflow 语法 OK、`python -m build` 产出 sdist+wheel 正常。
  - 首版 release job 冗余的 `gh release upload` 步骤与 softprops 的 files 列表重复 → 删除，统一走 softprops。
- **验证结果**：YAML 语法解析通过；本地 `python -m build` 成功产出 `musiclab-0.1.0-py3-none-any.whl` 与 tar.gz（dist/ 已在 .gitignore）。
- **遗留 / 下一步**：①真实触发验证：推 `v0.1.0` tag 观察 Actions 全链路（尤其 Tauri deb 构建）；②PyInstaller 打包 `musiclab serve` sidecar 产物接入 `externalBin`；③插件系统。