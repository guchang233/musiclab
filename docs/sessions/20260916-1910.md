## 2026-09-16 19:1X - Release 流水线补 Windows exe 并全链路跑通（v0.1.1）

- **目标**：打包 exe（PyInstaller）并发布到 Release；新 tag 出现时自动触发。
- **上下文锚点**：CI 已绿（`f213d79`）；release.yml 已有 test→build→release 骨架。
- **关键改动**：
  - `release.yml`：新增 `build-windows` job（windows-latest，PyInstaller onefile 打包 `musiclab.exe`，`--collect-all librosa/soundfile/mido`，构建后 `backends` 冒烟测试，`if-no-files-found: error`）；release job 附 `exe/musiclab.exe`。
  - `musiclab/cli.py`：main() 入口将 stdout/stderr `reconfigure(encoding="utf-8", errors="replace")`——修复 Windows cp1252 控制台打印中文表格的 `UnicodeEncodeError`（exe 用户必现崩溃）。
  - `ui/web/`（新）：前端静态资源从 `ui/` 根挪入独立目录，`frontendDist: "../web/"`——修复 tauri build 拒绝「dist 目录包含 src-tauri/target」。
  - `ui/src-tauri/icons/icon.png`（新，Pillow 生成波形主题 512px）+ `tauri.conf.json` `bundle.icon`——修复 generate_context 缺图标编译失败。
  - `.gitignore`：`ui/src-tauri/target/`、`ui/src-tauri/gen/`。
  - 顺带修复 `main()` 引用 `_build_parser` 的函数名错误（CI 之外未暴露）。
- **决策与理由**：
  - exe 修复编码问题选「改 CLI 代码」而非「CI 设 PYTHONUTF8=1」：真实 Windows 用户双击 exe 同样会崩，代码级修复一劳永逸。
  - build-windows 为阻塞 job（exe 是发布核心诉求）；build-tauri 保持 continue-on-error。
- **踩坑 / 修复**（三次失败的根因链）：
  1. v0.1.0 首跑：Windows 冒烟 `UnicodeEncodeError`（cp1252）→ cli 入口 UTF-8 重配。
  2. 同跑 Tauri：frontendDist 含 src-tauri → 前端挪 `ui/web/`。
  3. v0.1.0 重打：Tauri 仍败于缺 `icons/icon.png` → 补图标 + bundle.icon。
- **验证结果**：
  - 本地：PyInstaller Linux 版冒烟（backends / serve / HTTP 任务 / MIDI 产出 37 音符）全通，122 用例通过。
  - CI：`v0.1.0` run 35129032101 success（exe 发布）；`v0.1.1` run 35130081944 全 job success。
  - Release v0.1.1 资产：`musiclab.exe` 103.7MB、`MusicLab_0.1.0_amd64.deb` 2.7MB、whl、tar.gz。
- **遗留 / 下一步**：
  - **版本号约定**：whl/deb 里版本取自 pyproject/tauri.conf（0.1.0），与 tag（v0.1.1）不一致——以后打 tag 前先同步 bump `pyproject.toml` 与 `tauri.conf.json` 的 version（或加自动同步步骤）。
  - exe 内 ML 后端（demucs/basic-pitch）未打进：torch 体积巨大，后续可选 `--collect-all` 加大包或做成可选下载。
  - PyInstaller exe 与 Tauri 壳的 sidecar 对接（externalBin）。