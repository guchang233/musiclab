## 2026-09-16 17:5X - 阶段②：核心服务进程（任务队列 + HTTP/SSE API）+ Tauri 壳骨架

- **目标**：实现三层进程模型的中层——UI 与引擎之间的核心服务进程，及 UI 壳骨架。
- **上下文锚点**：上轮已完成引擎层 + 122 用例基线（当时 98）；commit `74da156`（AGENTS.md 留痕机制）。
- **关键改动**：
  - `musiclab/pipeline/pipeline.py`：`run()` 新增 `on_event`（阶段进度事件）与 `should_cancel`（协作式取消）钩子，阶段边界检查取消。
  - `musiclab/engine/errors.py` + `engine/__init__.py`：新增 `OperationCancelledError` 并导出。
  - `musiclab/service/tasks.py`（新）：Task 状态机（queued→running→succeeded/failed/cancelled）、TaskManager（worker 线程池 + 事件总线 + 订阅/毒丸机制）、提交期参数校验、queued/running 两级取消。
  - `musiclab/service/api.py`（新）：纯标准库 HTTP API——`/api/health`、`/api/backends`、`POST/GET /api/tasks`、`GET /api/tasks/{id}[?events=1]`、SSE `/api/tasks/{id}/events`、`POST .../cancel`；结构化 JSON 错误（400/404/409）。
  - `musiclab/cli.py`：新增 `serve` 子命令（`musiclab serve --host --port --workers`）。
  - `ui/`（新）：Tauri 壳骨架——原生 JS 前端（提交/列表/进度条/SSE 实时进度）+ `src-tauri`（Rust 拉起 `musiclab serve` sidecar，`MUSICLAB_SERVE_CMD` 可覆盖，退出回收子进程）。
  - `tests/test_service_tasks.py`、`tests/test_service_api.py`（新）：24 用例。
- **决策与理由**：
  - API 用 stdlib `http.server` 而非 FastAPI/ZeroMQ：服务层零新增依赖，契约稳定可测；未来可平移到 ZeroMQ 而不动 manager。
  - 取消语义两级：queued 直接取消；running 走 pipeline 阶段边界协作式取消（`OperationCancelledError`），避免强杀线程。
  - 事件按任务留痕（上限 2000 条）+ SSE 先回放后增量，断线重连不丢上下文。
  - UI 壳只做呈现与调度，业务全在服务进程——壳可整体替换（浏览器直接打开 `ui/index.html` 也能用）。
- **踩坑 / 修复**：
  - 沙箱环境被重置、依赖全丢 → 重新 `pip install` numpy/scipy/librosa/soundfile/mido/pytest。
  - 分轨被路由跳过时 `report.stages` 无对应 StageReport → `next(...)` 抛 StopIteration → 改为 `next((...), None)` + `skipped=True` 事件。
  - `musiclab.service.__init__` 忘导出 `serve` → 测试收集 ImportError → 补导出。
  - 后端实际名为 `spectral-hpss` 而非 `spectral` → 修测试断言。
  - Tauri `State<Sidecar>` 需要 `Mutex<Child>` 才能在事件回调里 kill → 包 `Mutex`。
- **验证结果**：`pytest -q` 122 passed（98 旧 + 24 新）。HTTP 端到端：19.7s 合成曲 pipeline 任务约 5s 完成（spectral-hpss + pyin/drum-onset），4 轨 MIDI 产出（other 因缺 basic-pitch 按能力矩阵跳过并告警，符合预期）；SSE 回放/实时流正常；终态任务取消返回 409。
- **遗留 / 下一步**：①Tauri 壳在本沙箱无法编译验证（需 webkit2gtk），首次构建需在有系统依赖的机器上做；②PyInstaller 打包 `musiclab serve` 产物并接入 `externalBin`；③插件系统（变调/降噪/和弦识别）仍待做；④ML 后端（demucs/basic-pitch）安装后的服务层集成验证。