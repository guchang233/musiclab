## 2026-09-16 16:5X - GitHub 推送 + 建立会话留痕机制

- **目标**：把 `musiclab` 推送到 GitHub；创建项目规则文件，强制每轮 AI 会话结束留痕保存上下文。
- **上下文锚点**：上一会话已完成核心引擎（分离/转录/管线/缓存）+ 98 用例跑通，工作目录 `/workspace/musiclab`（尚未 git init）。
- **关键改动**：
  - `.gitignore`：排除 `__pycache__/`、`*.egg-info/`、`*.wav`、`out/`、`cache/` 等。
  - `README.md`：项目简介、分层架构、安装/使用/测试说明。
  - `AGENTS.md`：项目规则文件，含代码规范 + 「会话留痕（强制）」机制。
  - `docs/sessions/`：`log.md`（索引）、`TEMPLATE.md`（模板）、本条留痕文件。
- **决策与理由**：
  - 规则文件用 `AGENTS.md`（通用 Agent 约定位置），Agent 开工必读。
  - 留痕采用「时间戳明细文件 + log.md 倒序索引」，避免单文件无限膨胀、支持并发会话隔离。
- **踩坑 / 修复**：
  - 目录非 git 仓库 → 先 `git init` 再推。
  - 全局无 `user.name/email` → 用环境变量提供瞬时身份提交，未改全局 git config。
- **验证结果**：仓库 `guchang233/musiclab` 已建，commit `053116d` 推送成功并经 API 复核；提交后已移除 remote 中的 token。
- **遗留 / 下一步**：①搭 Tauri UI 壳 + sidecar 骨架；②扩展插件后端（变调/降噪/和弦识别）；③建议用户撤销本次使用的 GitHub token。