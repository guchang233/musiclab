# 会话留痕模板

复制此模板，重命名为 `<YYYYMMDD-HHMM>.md` 后填写。
缺省字段写 `无`。保持简洁，聚焦决策与结果。

```markdown
## <会话时间> - <一句话目标>

- **目标**：<本轮任务>
- **上下文锚点**：<进入会话时的关键状态，如待办、分支、最近提交 sha>
- **关键改动**： <路径+做法，逐条列出>
- **决策与理由**：<架构/技术取舍>
- **踩坑 / 修复**：<错误现象 → 根因 → 修复>
- **验证结果**：<测试、用例数、端到端数据>
- **遗留 / 下一步**：<待办、建议后续动作>
```